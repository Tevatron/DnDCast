# ── Server config (contains hashed password + session secret) ──────────
config.json

# ── Live data files ─────────────────────────────────────────────────────
data/scenes.json
data/adventures.json
data/campaigns.json
data/notes.json

# ── Media assets (large binary files, manage outside git) ──────────────
assets/images/*
assets/audio/*

# Keep directory placeholders
!assets/images/.gitkeep
!assets/audio/.gitkeep

# ── Session storage (persisted login state, never commit) ───────────────
sessions/

# ── Playwright test artifacts ────────────────────────────────────────────
playwright-report/
test-results/
.last-run.json

# ── Dependencies ────────────────────────────────────────────────────────
node_modules/

# ── Private notes / local config docs (never commit) ────────────────────
private/

# ── OS / editor noise ───────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
